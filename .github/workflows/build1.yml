name: lab1

on:
  push:

  pull_request:

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: make
      run: |
        cd lab1/ 
          make maintest.c
          ./test
